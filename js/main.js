/* AgentQA — main entry point
   Imports and initialises all page modules. */

import { initNav } from './modules/nav.js';

document.addEventListener('DOMContentLoaded', function () {
  initNav();
});
