export default function ValueProp() {
  return (
    <section className="bg-gray-900 py-10 px-6">
      <p className="max-w-3xl mx-auto text-center text-gray-300 text-base leading-relaxed">
        As AI agents take on real business decisions, &ldquo;it worked in my test&rdquo; is no
        longer enough. AgentQA gives engineering and AI teams a governance layer that makes AI
        behavior observable, testable, and auditable at every stage of the deployment lifecycle.
      </p>
    </section>
  );
}
